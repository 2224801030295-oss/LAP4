name: lab4
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Icons iOS
  cupertino_icons: ^1.0.8

  # ⭐ Packages cần cho Weather App
  provider: ^6.1.2
  flutter_dotenv: ^5.1.0
  http: ^1.1.0
  geolocator: ^10.0.0
  geocoding: ^2.1.0
  shared_preferences: ^2.2.2
  connectivity_plus: ^4.0.2
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
    - .env
